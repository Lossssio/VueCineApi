// Importar la aplicación y el componente principal
import { createApp } from 'vue';
import App from './App.vue'; // Asumiendo que App.vue es tu componente principal

// Crear la aplicación Vue
const app = createApp(App);

// Montar la aplicación en el elemento con el ID "app"
app.mount('#app');
